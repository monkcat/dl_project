# ─── Core compute ───────────────────────────────────────
# torch: install separately for CUDA-matching build:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision

numpy>=1.24
pandas>=2.0
pillow>=10.0

# ─── Model / training ───────────────────────────────────
transformers==4.57       # SigLIPv2 support
peft>=0.10               # LoRA for SigLIP fine-tuning

# ─── HF data download + upload ──────────────────────────
huggingface_hub>=0.22

# ─── Eval / utilities ───────────────────────────────────
matplotlib>=3.8          # learning curve plots
pytz                     # docling timezone (graph builder)

# ─── Graph visualization (optional, eval/analysis only) ──
pyvis>=0.3
